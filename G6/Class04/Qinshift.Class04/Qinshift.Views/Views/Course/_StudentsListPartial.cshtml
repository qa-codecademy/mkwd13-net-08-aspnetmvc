@model List<StudentInfoViewModel>

@if (Model.Count == 0)
{
	<p class="text-danger">No students enrolled in this course.</p>
}
else
{
	<ul>
		@foreach (var student in Model)
		{
			<li>@student.FirstName @student.LastName - @student.Age</li>
		}
	</ul>
}